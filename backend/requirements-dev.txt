# Development & CI tooling. Installed on top of requirements.txt.
-r requirements.txt

pytest==8.3.4          # Test runner.
pytest-asyncio==0.25.2 # Async test support for future async routes/services.
httpx==0.28.1          # Required by FastAPI's TestClient; async HTTP client.
black==24.10.0         # Opinionated code formatter.
ruff==0.9.2            # Fast linter.
mypy==1.14.1           # Static type checker.
reportlab==4.2.5       # DEV-ONLY: generates synthetic test PDFs; never used in prod.
